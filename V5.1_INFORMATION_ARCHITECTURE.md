# V5.1 统一信息架构设计规范 (Information Architecture Specification)

> 版本：V5.1.0  
> 定位：CppAIService & muduo 个人工程学习工作台  
> 目标周期：未来 2 个月 C++ / Linux / muduo / CppAIService 深度研发与求职冲刺  
> 设计哲学：**Engineering Plain Style**（少功能、低认知负担、真实记录、清晰因果、10秒直达）

---

## 目录
1. [全局导航与视图架构](#1-全局导航与视图架构)
2. [首页 5 大核心区域与 DOM 布局规划](#2-首页-5-大核心区域与-dom-布局规划)
3. [统一 Daily Task 数据模型](#3-统一-daily-task-数据模型)
4. [EngineeringWorkLog 工程工作日志模型](#4-engineeringworklog-工程工作日志模型)
5. [CppAIService 模块层级地图 (ModuleHierarchyMap)](#5-cppaiservice-模块层级地图-modulehierarchymap)
6. [页面状态与 localStorage 字段定义与平滑迁移](#6-页面状态与-localstorage-字段定义与平滑迁移)
7. [模态框与交互弹窗收敛方案](#7-模态框与交互弹窗收敛方案)
8. [文案与设计语言整改清单](#8-文案与设计语言整改清单)
9. [验收标准与落地保障](#9-验收标准与落地保障)

---

## 1. 全局导航与视图架构

### 1.1 导航层级精简设计
当前系统存在 10 个平级导航按钮，且部分按钮带有装饰性营销词（如“双核中枢”、“4-Hop穿透”、“P7排程”等）。
V5.1 将导航收敛为 **主工作台 + 3 大核心工程视图 + 2 个辅助视图**，右侧常驻设置与数据备份。

- **主工作台 (P0，每日高频)**：
  - `工作台 (Home)` (`#nav-dashboard` -> `#view-dashboard`)：首页 5 大核心区域（今日任务、项目进度、学习进度、最近记录、本周统计）。
- **核心工程视图 (P1，每日编码与复盘)**：
  - `CppAIService 项目` (`#nav-project` -> `#view-project`)：树状模块地图、源码关联、任务联动、测试用例验证（由原 `#view-mapping` 升级重构）。
  - `muduo 源码路线` (`#nav-daily` -> `#view-daily`)：28 天学习路径、源码拆解与关键坑点。
  - `知识库 (语雀)` (`#nav-yuque` -> `#view-yuque`)：语雀目录索引、外部文档快速阅读器（保持既有结构，完全不破坏）。
  - `工程日志与求职` (`#nav-career` -> `#view-career`)：真实工程工作日志流、9类工程凭证、STAR 面试题库、核心技能项。
- **辅助视图 (P2，按需进入)**：
  - `日程同步` (`#nav-scheduler` -> `#view-scheduler`)：Google Tasks / Calendar ICS 真实数据导入、日程冲突展示、可用工时分析。
  - `代码测验` (`#nav-quiz` -> `#view-quiz`)：C++ / Linux 章节测验与题目复习。
- **全局工具 (顶部右侧)**：
  - `数据管理` (`#btn-settings` / `#btn-export` / `#btn-import`)：导入/导出 JSON 备份与重置。

---

## 2. 首页 5 大核心区域与 DOM 布局规划

彻底剥离首页高宽达 3000px 的 3 套 700 行静态 SVG 拓扑图、7 个重复计数卡片与无意义雷达图。
首页整体采用清晰的 **两列网格布局**（左侧宽栏聚焦执行与任务，右侧紧凑栏聚焦进度与统计），高度控制在 1~2 屏以内，严格满足 **“打开页面 10 秒内明确今日动作”**。

```
+-----------------------------------------------------------------------------------+
|  Header: CppAIService & muduo 个人工程工作台  [当前聚焦: 阶段三 HTTP与服务集成] [导出备份] |
+---------------------------------------------------------+-------------------------+
|  左侧主操作区 (col-span-8)                                | 右侧状态区 (col-span-4)  |
|                                                         |                         |
|  [区域 1：今日任务 (Today's Tasks)] 预计 5.5h             | [区域 2：项目进度]       |
|  - [S] CppAIService: 实现 HttpContext 状态机解析 (2.5h)  | CppAIService 模块进度   |
|  - [A] muduo: 研读 EventLoop 事件分发流程 (1.0h)         | 当前攻坚: HTTP Router    |
|  - [A] 算法: LeetCode 207 拓扑排序 (1.0h)                | 已通过测试: 4/12 模块   |
|  - [B] 八股/复习: 整理 Reactor 与 Proactor 对比 (1.0h)   +-------------------------+
|  [+ 快速添加今日任务]  [打卡并触发反思记录]               | [区域 3：muduo 学习进度] |
|                                                         | 当前进度: Day 12 / 28   |
|  +----------------------------------------------------+ | 本周目标: EventLoop核心 |
|  | [区域 4：最近工程记录 (Recent Logs)]                 | 源码完成度: 42%         |
|  | 2026-09-12 [Bug修复] Router 正则匹配失效 (已解决)    +-------------------------+
|  | 2026-09-11 [代码实现] 封装 Buffer 自动扩容逻辑        | [区域 5：本周统计]       |
|  | 2026-09-10 [Demo] 编写 EchoServer 吞吐测试脚本       | - 学习工时: 18.5h       |
|  | [+ 编写新工作日志]                                  | - 项目工时: 12.0h       |
|  +----------------------------------------------------+ | - 算法题数: 14 题       |
|                                                         | - 完成任务: 21 项       |
|                                                         | - 真实凭证: 6 条        |
+---------------------------------------------------------+-------------------------+
```

### 2.1 DOM 结构定义与挂载点

```html
<!-- index.html 首页结构规范 -->
<section id="view-dashboard" class="space-y-6">
  
  <!-- 顶部状态栏：简明扼要，拒绝花哨 -->
  <header class="flex items-center justify-between pb-4 border-b border-gray-800">
    <div>
      <h1 class="text-xl font-bold text-gray-100">工程学习工作台</h1>
      <p class="text-xs text-gray-400 mt-1">主线：muduo 网络库源码 ➔ CppAIService 框架研发 ➔ 求职工程凭证</p>
    </div>
    <div class="flex items-center space-x-3 text-xs">
      <span class="px-2.5 py-1 rounded bg-blue-950 text-blue-400 border border-blue-800" id="header-current-focus">
        当前聚焦：阶段三 HTTP 协议栈实现
      </span>
      <button onclick="openWorkLogModal()" class="px-3 py-1.5 rounded bg-emerald-700 hover:bg-emerald-600 text-white font-medium">
        + 记工作日志
      </button>
    </div>
  </header>

  <!-- 首页 5 大核心区域栅格 -->
  <div class="grid grid-cols-1 lg:grid-cols-12 gap-6">
    
    <!-- 左侧主栏 (8 列) -->
    <div class="lg:col-span-8 space-y-6">
      
      <!-- 区域 1：今日任务 -->
      <div id="home-zone-today-tasks" class="bg-gray-900 border border-gray-800 rounded-lg p-5">
        <div class="flex items-center justify-between mb-4">
          <div class="flex items-center space-x-2">
            <h2 class="text-base font-semibold text-gray-100">今日任务</h2>
            <span id="today-tasks-summary" class="text-xs text-gray-400">预计 5.5h · 2/5 已完成</span>
          </div>
          <div class="flex items-center space-x-2">
            <button onclick="openQuickAddTaskModal()" class="text-xs text-blue-400 hover:text-blue-300">
              + 添加任务
            </button>
            <span class="text-gray-600">|</span>
            <button onclick="syncCalendarTasksToToday()" class="text-xs text-gray-400 hover:text-gray-300">
              拉取外部日程
            </button>
          </div>
        </div>
        <!-- 任务列表容器 -->
        <div id="today-tasks-list" class="space-y-2">
          <!-- 动态渲染 UnifiedTask 列表 -->
        </div>
      </div>

      <!-- 区域 4：最近工程记录 -->
      <div id="home-zone-recent-logs" class="bg-gray-900 border border-gray-800 rounded-lg p-5">
        <div class="flex items-center justify-between mb-4">
          <div class="flex items-center space-x-2">
            <h2 class="text-base font-semibold text-gray-100">最近工程记录</h2>
            <span class="text-xs text-gray-400">最近 5 条开发/排障/笔记记录</span>
          </div>
          <button onclick="switchView('career')" class="text-xs text-blue-400 hover:text-blue-300">
            查看全部记录 &rarr;
          </button>
        </div>
        <!-- 记录列表容器 -->
        <div id="recent-logs-list" class="divide-y divide-gray-800">
          <!-- 动态渲染 5 条最新 EngineeringWorkLog -->
        </div>
      </div>

    </div>

    <!-- 右侧辅助栏 (4 列) -->
    <div class="lg:col-span-4 space-y-6">
      
      <!-- 区域 2：项目进度 (CppAIService) -->
      <div id="home-zone-project-progress" class="bg-gray-900 border border-gray-800 rounded-lg p-5">
        <div class="flex items-center justify-between mb-3">
          <h2 class="text-base font-semibold text-gray-100">CppAIService 进度</h2>
          <button onclick="switchView('project')" class="text-xs text-blue-400 hover:text-blue-300">模块地图 &rarr;</button>
        </div>
        <div id="project-progress-content" class="space-y-3 text-xs text-gray-300">
          <!-- 当前阶段、当前攻坚模块、代码完成度、测试覆盖率 -->
        </div>
      </div>

      <!-- 区域 3：muduo 学习进度 -->
      <div id="home-zone-muduo-progress" class="bg-gray-900 border border-gray-800 rounded-lg p-5">
        <div class="flex items-center justify-between mb-3">
          <h2 class="text-base font-semibold text-gray-100">muduo 源码进度</h2>
          <button onclick="switchView('daily')" class="text-xs text-blue-400 hover:text-blue-300">28天路线 &rarr;</button>
        </div>
        <div id="muduo-progress-content" class="space-y-3 text-xs text-gray-300">
          <!-- Day X / 28，本周主题，源码模块研读进度 -->
        </div>
      </div>

      <!-- 区域 5：本周统计 (纯净事实指标) -->
      <div id="home-zone-weekly-metrics" class="bg-gray-900 border border-gray-800 rounded-lg p-5">
        <h2 class="text-base font-semibold text-gray-100 mb-3">本周统计</h2>
        <div class="grid grid-cols-2 gap-3 text-center" id="weekly-metrics-grid">
          <!-- 学习工时、项目工时、算法题数、完成任务、工程凭据 -->
        </div>
      </div>

    </div>

  </div>
</section>
```

---

## 3. 统一 Daily Task 数据模型

### 3.1 消除 4 处数据孤岛
在 V5.0 中，任务数据分散在：
1. `dailyRoutine` (7 项写死右侧栏)
2. `schedulerSystem.dailySchedule.scheduledBlocks` (调度引擎)
3. `mastery.tasks` (28 天学习打卡)
4. `#view-dashboard` (推荐卡片)

在 V5.1 中，建立唯一权威的 **`unifiedTasks`** 运行时与持久化数组。每日任务统一汇入该表，右侧栏及首页均从此读取。

### 3.2 规范接口定义

```typescript
export interface UnifiedDailyTask {
  id: string;               // 唯一 ID，例如 "task-20260912-http-01"
  title: string;            // 任务标题，例如 "编写 HttpContext 状态机解析与单元测试"
  priority: 'S' | 'A' | 'B' | 'C'; // 优先级：S(项目核心), A(源码/算法), B(八股/自测), C(杂项)
  category: 'project' | 'muduo' | 'algo' | 'theory' | 'other';
  estimatedMinutes: number; // 预估耗时（分钟）
  actualMinutes?: number;   // 实际耗时（打卡时填入）
  moduleRef?: string;       // 关联项目模块（如 "CppAIService::HTTP::HttpContext"）
  sourceRef?: string;       // 关联源码（如 "muduo/net/http/HttpContext.cc"）
  completed: boolean;       // 是否完成
  completedAt?: string;     // 完成时间 ISO 字符串
  
  // 下一步驱动机制 (关键创新: 拒绝打卡即结束)
  reflection?: {
    learned: string;        // 本次学到了什么关键点
    problem: string;        // 遇到了什么卡点/问题
    solution: string;       // 如何解决的
    nextStep: string;       // 下一步需要衔接的动作
  };

  source: 'system_preset' | 'user_manual' | 'google_calendar' | 'google_tasks';
  date: string;             // 日期 "YYYY-MM-DD"
}
```

### 3.3 优先级保障规则
- 每日总预估工时控制在 **5.5h ~ 6.5h**（防止虚高定级与严重精力透支）。
- 当日时间不足时：
  - **S 级项目任务 (2.5h~3.0h)**：严格保留，不得自动取消。
  - **A 级源码/算法 (1.0h~2.0h)**：保证最小 1.0h 底线。
  - **B/C 级 (阅读/杂项)**：提示“今日时间不足时，优先保证项目任务”，建议顺延并记录原因。

---

## 4. EngineeringWorkLog 工程工作日志模型

### 4.1 设计意图
用户求职和技术成长的核心凭证不是“我打卡了”，而是：
**做了什么代码 ➔ 遇到了什么 Bug ➔ 如何排查定位 ➔ 获得了什么真实结论 ➔ 下一步做什么**。
每条日志均可一键转换为 **面试复述案例 (STAR 故事)** 或 **工程凭证 (Evidence)**。

### 4.2 规范接口定义

```typescript
export interface EngineeringWorkLog {
  id: string;               // "log-1726123456789"
  date: string;             // "2026-09-12"
  project: 'CppAIService' | 'muduo' | 'LeetCode' | 'Linux' | 'Other';
  module: string;           // 例如 "HTTP Router" / "EventLoop" / "ThreadLocal"
  logType: 'bugfix' | 'code_feature' | 'demo_experiment' | 'source_study' | 'benchmark';
  
  what: string;             // 今天完成了什么具体工作
  problem: string;          // 遇到的核心问题或卡点（没有写“无”）
  solution: string;         // 解决方案与定位过程
  learned: string;          // 核心收获或底层原理心得
  next: string;             // 下一步明确动作
  
  relatedFiles?: string[];  // 涉及代码文件，例如 ["src/http/Router.cpp", "tests/Router_test.cpp"]
  interviewPoint?: string;  // 可用于面试回答的点（例如 "能解释 HTTP 状态机解析与非阻塞读写边缘条件"）
  createdAt: number;        // 时间戳
}
```

### 4.3 呈现形式
- 首页展示最近 5 条卡片式日志，包含标签 `[Bug修复]`、`[代码实现]`、`[Demo验证]`。
- 支持单条日志展开查看代码变更、问题因果分析，并提供“生成面试卡片”快捷按钮。

---

## 5. CppAIService 模块层级地图 (ModuleHierarchyMap)

### 5.1 降级并替换 700 行 SVG
原首页嵌入的 3 份庞大 SVG（`id="arch-svg"`, `id="muduo-svg"`, `id="flow-svg"`）占用大量代码体积且在不同分辨率下严重错位。
V5.1 将其从首页移除，迁移至 `#view-project`（原 `#view-mapping` 改造），采用 **文本树状结构 + 紧凑交互节点卡片**。

### 5.2 核心模块树状定义

```
CppAIService (高性能 C++ AI 推理服务框架)
├── 1. Network & Protocol (底层网络与协议栈) [基于 muduo 改造]
│   ├── HttpContext.cpp      [状态机解析: 请求行/请求头/请求体]
│   ├── HttpRequest.h        [请求参数抽取/Header映射]
│   ├── HttpResponse.cpp     [HTTP响应格式化/分块传输]
│   └── Router.cpp           [前缀树/哈希路由分发与中间件链]
│
├── 2. Core Engine (核心服务引擎)
│   ├── Server.cpp           [主从 Reactor 线程池驱动]
│   ├── ConnectionPool.cpp   [下游连接复用与生命周期管理]
│   └── Buffer.cpp           [自动扩容与零拷贝切片设计]
│
├── 3. MCP & Tool Invocation (模型上下文协议集成)
│   ├── McpRegistry.cpp      [工具注册表与元数据描述]
│   ├── ToolCaller.cpp       [入参校验与异步调用封装]
│   └── JsonRpcHandler.cpp   [JSON-RPC 2.0 协议打包与解包]
│
├── 4. AI Inference & Integration (推理后端对接)
│   ├── ModelClient.cpp      [大模型 HTTP/gRPC 流式客户端]
│   ├── StreamParser.cpp     [SSE Server-Sent Events 流式增量解析]
│   └── ContextCache.cpp     [多轮对话上下文缓存与 LRU 淘汰]
│
└── 5. Middleware & Message Queue (中间件与通信)
    ├── RabbitMQProducer.cpp [异步任务发布与离线任务解耦]
    └── MetricsCollector.cpp [请求吞吐/时延 P99 打点统计]
```

### 5.3 交互动作
每个模块节点均提供 4 个直接动作：
1. **源码跳转**：查看代码文件路径与关键实现函数。
2. **关联任务**：筛选包含该模块的今日/历史任务。
3. **面试题**：关联关于该模块的高频考察点（如 “HTTP 状态机粘包处理”）。
4. **Demo / 测试**：显示对应的单元测试和自测验证用例。

---

## 6. 页面状态与 localStorage 字段定义与平滑迁移

### 6.1 保证 100% 数据向下兼容
系统主存储键名保持为：`localStorage['muduo_v6_data']`（以及旧版兼容 `muduo_v5_data`）。
版本号平滑升级至 `"6.1.0"`。

### 6.2 状态结构规范 (V6.1)

```javascript
{
  "version": "6.1.0",
  "updatedAt": "2026-09-12T...",
  
  // 保持原有模块结构不变，防止历史数据损毁
  "learning": { ... },
  "mastery": { "tasks": { ... }, "days": { ... } },
  "pitfalls": { ... },
  "quiz": { ... },
  "careerSystem": { ... },     // 原 Phase 6 凭据与面试数据完整保留
  "schedulerSystem": { ... },  // 原 Phase 7 日历与排程设置完整保留

  // V5.1 新增核心字段 (严格增量扩展)
  "workLogs": [
    // 存放 EngineeringWorkLog[] 实例
  ],
  "unifiedTasks": [
    // 存放 UnifiedDailyTask[] 实例
  ],
  "projectProgress": {
    "currentStage": "阶段三：HTTP 与应用层集成",
    "activeModule": "HTTP Router",
    "completedModules": ["Buffer", "HttpContext", "HttpRequest", "HttpResponse"],
    "inProgressModules": ["Router"],
    "totalModules": 12
  }
}
```

### 6.3 数据迁移策略 (`_migrateToV51`)
在 `StateManager.init()` 中执行迁移校验：
1. 检测当前数据是否含有 `workLogs`，若无则初始化为 `[]`。
2. 从现存 `careerSystem.evidences` 中提取历史具有代表性的记录，自动逆向生成对应的初始 `workLogs`，让用户一打开即有真实数据。
3. 检测 `unifiedTasks`，若无则自动从原 `dailyRoutine` 和 `schedulerSystem` 合并生成今日初始任务列表。
4. 绝不删除旧键，确保即使发生回滚也不会丢失任何内容。

---

## 7. 模态框与交互弹窗收敛方案

### 7.1 收敛现状
原有 14 个全局模态框造成弹窗泛滥，严重割裂操作连贯性。

| 原模态框 | 收敛策略 | V5.1 实施方案 |
| :--- | :--- | :--- |
| `#calendar-import-modal` | 保留 | 归纳至“日程同步”视图的侧边抽屉或简洁弹窗 |
| `#task-diagnostic-modal` | 合并 | 与任务完成/未完成打卡合并，直接在任务项内折叠展开展开填写根因 |
| `#modal-create-evidence` | 合并 | 由 `#modal-work-log` 统一承载，勾选“同步生成求职凭证”即可生成，无需二次录入 |
| `#modal-rubric-detail` | 内联化 | 直接在求职视图中展示卡片抽屉，去除全屏弹窗 |
| `#modal-star-detail` | 内联化 | 在面试题目下方直接展开折叠层 |
| `#modal-4hop-detail` | 内联化 | 在模块详情页下直接呈现联动面板 |
| `#modal-export-data` | 保留 | 核心备份工具，保持极简 |
| `#modal-import-data` | 保留 | 核心恢复工具，保持极简 |
| `#modal-reset-data` | 保留 | 核心重置工具，带确认防误触 |

### 7.2 新增核心模态框：`#modal-work-log`
一站式记录工程日志与打卡反思，结构紧凑：
- 选择所属项目与模块 (`CppAIService`, `muduo`, `LeetCode` 等)
- 今日完成了什么
- 遇到什么问题（若无写“无”）
- 怎么解决的
- 下一步计划
- [可选勾选] 同步至求职凭证库

---

## 8. 文案与设计语言整改清单

### 8.1 禁用虚浮词汇对照表
全面执行 **Engineering Plain Style**，严禁一切营销炒作与虚假浮夸词汇：

| 禁用词汇 (违规) | 替换为 (事实/工程规范) |
| :--- | :--- |
| DUAL-CORE OS / 双核工程中枢 | CppAIService & muduo 个人工程工作台 |
| 工业级 / 企业级 | 实装代码 / 经测试验证 |
| 闭环 / 全链路 / 穿透 | 关联 / 联动 / 从源码到面试 |
| 异步削峰 (无测试数据时) | 异步传递任务 / 缓冲队列 |
| Reactor 心脏 / 动力引擎 | 运行事件循环并分发 I/O 事件 |
| 誓死保卫核心主干 | 优先保证项目任务完成 |
| 时间赤字压缩 | 压缩次要任务工时 |
| 赤字诊断 / 优先级流水线 | 调整任务时间 / 任务优先级 |
| 14 维能力雷达评定 | 核心技能项评估 (C++, Linux, 网络, 项目, 算法) |
| 极致 / 强大 / 颠覆 | (直接删除该形容词，保留客观事实) |

### 8.2 英文营销副标题清除表
彻底删除各主卡片下无实际操作意义的英文副标题：
- ✕ `UNIFIED LEARNING SYSTEM` ➔ 移除
- ✕ `CAREER EVIDENCE & CAPABILITY SYSTEM` ➔ 移除
- ✕ `PERSONAL SCHEDULER & REAL CALENDAR SYNC` ➔ 移除
- ✕ `SEQUENTIAL SOURCE CODE EXPLORATION` ➔ 移除
- ✕ `HIGH-CONCURRENCY RPC BACKBONE` ➔ 移除

---

## 9. 验收标准与落地保障

用户访问应用后，必须在 **10 秒内** 获得以下 6 个问题的确切答案：
1. **今天应该做什么？** ➔ 首页区域 1 展示 3~5 项排好优先级的具体任务及预估时长。
2. **我现在正在学什么？** ➔ 顶部状态栏及区域 3 清晰显示当前 muduo Day X/28 及核心概念。
3. **CppAIService 做到哪里？** ➔ 首页区域 2 明确标注当前阶段、当前攻坚模块与通过测试数。
4. **muduo 学到哪里？** ➔ 首页区域 3 进度条与源码模块研读进度。
5. **最近解决了什么问题？** ➔ 首页区域 4 最近 5 条工程记录直观展示 Bug 修复与实现心得。
6. **下一步是什么？** ➔ 最新一条日志或任务项下方明确指出的“下一步计划”。
